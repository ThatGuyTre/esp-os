# ESP32 Development

### Startup
To begin, you will need to run either `source ~/.espressif/tools/activate_idf_v6.0.sh` (change 6.0 to whatever version you are running if different)

Or simply `. ./activate.sh` as a shortcut

After activating, you can build using `idf.py build`